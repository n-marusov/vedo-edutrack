# Packages allowed to run postinstall scripts (pnpm v11).
# Must be in frontend/.npmrc — root .npmrc is not visible when
# running pnpm from a workspace member directory.
pnpm.onlyBuiltDependencies[]=@biomejs/biome
pnpm.onlyBuiltDependencies[]=esbuild
