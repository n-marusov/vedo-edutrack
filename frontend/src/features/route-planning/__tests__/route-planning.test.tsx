import { describe, expect, it } from 'vitest';

describe('<route-planning>', () => {
  it('should render the route-planning placeholder component', () => {
    expect.fail(
      'TODO: implement route-planning component tests (US-plan.compute.shortest-path / UC-plan.compute.shortest-path-to-goal (M1))',
    );
  });
});
