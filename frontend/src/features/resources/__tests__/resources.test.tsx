import { describe, expect, it } from 'vitest';

describe('<resources>', () => {
  it('should render the resources placeholder component', () => {
    expect.fail(
      'TODO: implement resources component tests (US-resource.catalog.filter-by-format / UC-resource.catalog.filter-by-format (S1))',
    );
  });
});
