package identityaccess

import "testing"

// TODO(M0.3): domain tests for the identityaccess bounded context (RBAC role catalog (T8) / REQ-NFR-security.compliance.role-catalog).
// Intentionally red at M0.2 (T13) — replace with real domain tests
// (ADR-IMPL.PROCESS.repository-structure §5).
func TestIdentityaccessDomainPlaceholder(t *testing.T) {
	t.Skip("TODO: implement domain tests for identityaccess (RBAC role catalog (T8))")
}
