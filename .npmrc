# Registry & auth configuration only (pnpm v11 — settings live in
# pnpm-workspace.yaml). No pnpm settings belong in this file.
# Keep tokens out of the repo; use ${VAR} expansion or the user-level
# auth file (pnpm login).
