# Traefik static configuration for VEDO EduTrack dev stack.
#
# Defines entrypoints, providers, and the API dashboard.
# Dynamic routing rules are in dynamic.yml.
#
# See ADR-IMPL.PROCESS.development-tooling §8 (Traefik reverse-proxy).

entryPoints:
  web:
    address: ":80"
    # HTTP->HTTPS redirect is disabled for the dev stack. The routers below
    # listen on both entryPoints; dev uses plain HTTP.
    # HOST port 9080 is mapped to this container port 80 to bypass browser
    # HSTS cache (localhost:80 cached for 1 year by earlier runs).
    # Re-enable for production:
    # http:
    #   redirections:
    #     entryPoint:
    #       to: websecure
    #       scheme: https
    #       permanent: true
  websecure:
    address: ":443"

# TLS certificates: Let's Encrypt (staging by default for dev, production
# endpoint in CI/production — override via ACME_CASERVER). Dev uses plain HTTP
# on port 9080 (tls is disabled in dynamic.yml). Enable tls: true on routers
# and the httpChallenge below will issue Let's Encrypt certificates.
# See ADR-IMPL.PROCESS.development-tooling §8.
certificatesResolvers:
  letsencrypt:
    acme:
      email: ${ACME_EMAIL:-admin@edutrack.local}
      storage: /etc/traefik/acme.json
      caServer: ${ACME_CASERVER:-https://acme-staging-v02.api.letsencrypt.org/directory}
      httpChallenge:
        entryPoint: web

providers:
  docker:
    exposedByDefault: false
    network: edutrack-net
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

api:
  dashboard: true
  insecure: true  # dev-only; production uses TLS + auth

log:
  level: INFO
  format: json

accessLog:
  format: json

metrics:
  prometheus:
    addEntryPointsLabels: true
    addServicesLabels: true

# Blue-green deployment is a post-MVP weighted-services setup (see dynamic.yml
# comments); no additional static config is needed for it.
